def twoSum(nums, target):
   out=[];
   for i in range(len(nums)):
      first_num=nums[i];
      for j in range(i+1, len(nums)):
         sec_num=nums[j];
         if (first_num+sec_num==target):
            out.append(i);
            out.append(j);
   return out;
